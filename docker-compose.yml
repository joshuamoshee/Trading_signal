services:
  db:
    image: postgres:16
    environment:
      POSTGRES_USER: signalbot
      POSTGRES_PASSWORD: localdev
      POSTGRES_DB: signals
    ports:
      - "5432:5432"
    volumes:
      - db_data:/var/lib/postgresql/data

volumes:
  db_data: